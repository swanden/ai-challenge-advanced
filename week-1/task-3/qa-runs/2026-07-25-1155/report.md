# QA-прогон notes-api — 2026-07-25 11:55

Задача: `week-1/task-3/notes-api`. Оркестратор: `/qa`.
Артефакты прогона: `level1.md`, `level2.md`, `coverage.out`, `coverage.html`,
`screenshots/` (8 PNG), консольные логи браузера.

## Общий вердикт: ЗЕЛЁНЫЙ

Все три условия выполнены: ворота зелёные, профили покрытия на месте, скриншоты сняты и
шаги подтверждены снапшотами страницы (выборочно перепроверено оркестратором визуально).

---

## Уровень 1 — код-тесты

| пакет | было | стало |
|---|---|---|
| `internal/httpapi` | 0.0% | **100.0%** |
| `internal/note` | 47.1% | **94.1%** |
| `internal/storage` | 0.0% | **100.0%** |
| `cmd/notes-api` | 0.0% | 0.0% (только wiring) |
| **итого по модулю** | **7.0%** | **82.6%** |

Добавлены 4 файла: `internal/httpapi/handler_test.go`, `internal/note/service_test.go`,
`internal/note/deps_test.go`, `internal/storage/memory_test.go`. Продуктовый код не изменён —
проверено оркестратором: `git diff` по задаче пуст, в статусе только новые `*_test.go`.

**Мутации: 23 из 23 убиты.** Проверены статусы (400/404/500/201/204), маппинг доменных ошибок
через `errors.Is`, проброс `r.Context()` сквозь слои, `Content-Type`, лог ошибки кодирования,
карта маршрутов, `TrimSpace` и отказ на пустом тексте, `Clock`/`IDGenerator` вместо прямых
вызовов, `ErrNotFound` в репозитории, сортировка `List`, non-nil пустой срез, блокировка в
`Create` (ловится под `-race`). Детали — в `level1.md`.

Вердикт Уровня 1: **зелено**.

---

## Ворота (прогнаны оркестратором из корня репозитория)

| проверка | результат |
|---|---|
| `go build ./week-1/task-3/notes-api/...` | exit 0 |
| `go vet ./week-1/task-3/notes-api/...` | exit 0 |
| `gofmt -l week-1` | пустой вывод |
| `go test ./week-1/task-3/notes-api/... -count=1` | ok, exit 0 |
| `go test ... -race` (сверх обязательных) | ok, exit 0 |

Ворота **зелёные**, Уровень 2 запущен законно.

---

## Уровень 2 — smoke через браузер

Playwright MCP (headless), `http://localhost:8080/`, сервис поднят оркестратором и погашен
после прогона.

| сценарий | результат | подтверждение |
|---|---|---|
| S1. Создание заметки | PASS | `s1-01-open.png`, `s1-02-created.png` |
| S2. Несколько заметок и список | PASS (оговорка) | `s2-01-three-added.png` |
| S3. Удаление заметки | PASS | `s3-01-before-delete.png`, `s3-02-after-delete.png` |
| S4. Валидация пустого текста | PASS | `s4-01-empty-submit.png` + 400 в консольном логе |
| S5. Пустое состояние | PASS | `s5-01-deleting.png`, `s5-02-empty.png` |

**5 из 5.** Оговорка по S2: элементов в списке оказалось 4, а не 3 — заметка из S1 пережила
переход между сценариями, потому что хранилище в памяти сервиса не сбрасывается. Проверяемое
поведение (три новые заметки в порядке создания) подтверждено; это неточность формулировки
сценария, а не дефект.

Оркестратор перепроверил самостоятельно: все 8 PNG существуют и непусты; `s4-01` и `s5-02`
открыты и просмотрены — красная ошибка «Текст заметки обязателен» при неизменившемся списке
и пустое состояние «Пока нет заметок» соответствуют заявленному; 400 на `POST /notes` найден
в консольном логе своим `grep`.

Вердикт Уровня 2: **зелено**.

---

## Что стоит поправить (ничего не блокирует)

1. `docs/smoke-scenarios.md:18` — ожидание S2 «ровно три элемента» некорректно при накоплении
   состояния: переформулировать на «три новых элемента» или добавить шаг очистки.
2. `favicon.ico` → 404 на каждой загрузке страницы; лечится `<link rel="icon" href="data:,">`
   в `internal/httpapi/web/index.html`.
3. Рассинхрон `CLAUDE.md` и кода: документ приводит `"PATCH /notes/{id}"` как пример, а
   `Service`/`Handler` метода обновления не имеют — `PATCH` отдаёт 405, хотя
   `MemoryRepo.Update` реализован и покрыт тестами. Поведение зафиксировано тестом
   `TestHandlerRouting/PATCH_/notes/{id}_is_not_routed`.

## Состояние дерева после прогона

Служебные артефакты Playwright (`.playwright-mcp/`, `notes-api/qa-runs/`) убраны из каталога
задачи; PNG и логи перенесены в этот каталог прогона. В `git status` по задаче остались только
4 новых файла тестов.
