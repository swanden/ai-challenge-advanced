# Артефакты прогонов Дня 4

Нумерация соответствует разделам отчёта (`../README.md`).

| Файл | Что это |
|---|---|
| `ollama-capabilities.txt` | раздел 6 — проверка `tools` у обеих моделей и системный промпт Devstral с указанием на OpenHands |
| `proxyai-broken-handler.go.txt` | разделы 4.3–4.4 — `handler.go` после применения правок ProxyAI: маркеры патча в коде, дубли функции, осиротевшее тело |
| `proxyai-build-failure.txt` | вывод `go build` на этом файле и разметка повреждений по строкам |
| `aider-session.md` | раздел 4.7 — полный лог сессии Aider: промпт, план модели, диффы, подтверждение применения |
| `aider-result-task.go.txt` | доменный слой после Aider — метод `Search` написан по конвенциям проекта без замечаний |
| `aider-result-handler.go.txt` | транспортный слой после Aider — компилируется, но регистрирует невозможный маршрут |
| `aider-runtime-panic.txt` | паника при старте: `go build` и `go vet` чисты, ошибка вылезает только в рантайме |

Прогоны 4.5 и 4.6 (Claude Code на локальной модели) файловых артефактов не оставили —
в обоих случаях модель не тронула ни одного файла, результат приведён в отчёте цитатой.
